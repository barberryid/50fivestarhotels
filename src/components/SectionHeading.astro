---
interface Props {
  eyebrow?: string;
  title: string;
  subtitle?: string;
  align?: 'left' | 'center';
}

const { eyebrow, title, subtitle, align = 'left' } = Astro.props;
const textAlign = align === 'center' ? 'text-center' : 'text-left';
---

<div class={`${textAlign} mb-10`}>
  {eyebrow && (
    <p class="text-[#A0682E] text-xs font-bold uppercase tracking-widest mb-3">{eyebrow}</p>
  )}
  <h2 class="font-serif text-3xl md:text-4xl font-bold text-[#2A2622] leading-tight">{title}</h2>
  {subtitle && (
    <p class="text-[#5C544A] text-lg mt-4 leading-relaxed max-w-2xl">{align === 'center' ? '' : ''}{subtitle}</p>
  )}
</div>
